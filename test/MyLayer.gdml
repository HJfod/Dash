
import "MyLayer.hpp"

class MyAwesome {
    name: string;

    impl constructor(this) {
        this->name = "hi";
    }
    impl destructor(this) {}

    fun memberFun(this, value: string const&) -> void {
        this->name = value;
    }

    fun staticFun() {
    }
}

fun test() {
    let x = MyAwesome();
    x.memberFun("hi mom");
}
