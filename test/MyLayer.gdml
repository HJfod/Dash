
import "MyLayer.hpp"

fun print(text: string const&) {
    ```cpp
    std::cout << text << "\n";
    ```
}

class Something {
    name: string;
    value: i32;

    construct = {
        this->name = "ball man";
        this->value = 0;
    }
    construct { name: string const&, value: i32 } = default;

    destruct = {
    }

    fun getName(this) {
        return this->name;
    }

    fun staticFun() {
    }
}

fun test() {
    
    let x = Something {
        value: 0,
        name: "hi mom",
    };

    x.staticFun();
    print(x.getName());

}
