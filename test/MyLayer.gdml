
import "MyLayer.hpp"

namespace b {
    let z = 32;

    namespace c {
        fun addTwo(x: i32, y: i32) => x + y + z;
    }
}

using namespace b::c;

fun test() {
    let x = addTwo(5, 7);
    x += 2
}
