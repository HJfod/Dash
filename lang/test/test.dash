
let y = 5;

fun what(a: int, b: int) -> int {
    c;
    if a > b {
        return;
    }
    else {
        return a - b
    }
}

let x = what(2, 5) + 6;

assert((2 + 5 * 3 - 1 + 2 / 2) == (2 + (5 * 3) - 1 + (2 / 2)));
